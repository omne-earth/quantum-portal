# tunnel - the canonical TWO-MACHINE test: 1 lighthouse + 1 node = two VMs.
# The pqIX tunnel forms VM-to-VM over the real node network.
NODES=( "lh1:lighthouse" "n1:node" )
declare -A LH_SET=( [n1]="lh1" )
PORT=4242
OVL_PREFIX="10.99.0."
